:root {
  --main-color: #6f0029;
}

.AppComponent {
  padding: 1em;
  font-family: Arial, Helvetica, sans-serif;
  text-align: center;
  margin-top: 50px;
  display: block;
  color: var(--main-color);

  & .AppComponent__heading {
    font-size: 50px;
  }
}
